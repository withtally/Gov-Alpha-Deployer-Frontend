import { ComponentSingleStyleConfig } from '@chakra-ui/react';

const code: ComponentSingleStyleConfig = {
  variants: {
    unstyled: {
      p: 0,
      bg: 'transparent',
    },
  },
  defaultProps: {
    variant: 'unstyled',
  },
};

export default code;
